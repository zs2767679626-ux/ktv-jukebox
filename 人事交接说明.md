# 办公室 KTV 点歌台 · 交接说明

把本压缩包解压到这台电脑的任意文件夹（如 `D:\ktv-jukebox`），然后按下面步骤操作，全程约 15 分钟，之后每天开机自动运行，无需维护。

---

## 第一步：装两个免费软件（只装一次，已有可跳过）

1. **Node.js**（23.4 或更高版本）：https://nodejs.org/zh-cn 选 LTS 版，一路下一步
2. **Python**（3.9 或更高）：https://www.python.org/downloads/ 安装时**勾选 "Add python.exe to PATH"**

## 第二步：双击 `setup_lan_win.bat`

在解压出来的文件夹里双击 `setup_lan_win.bat`，按屏幕提示走：

- **设备口令**：直接回车（使用默认口令），或者自己输入一个，记住它即可
- 首次会联网安装依赖，等几分钟
- 防火墙放行会自动处理；如果提示失败，等会儿第一次启动服务器时 Windows 弹窗点「允许访问」
- 脚本会自动注册**开机自启**，屏幕上有提示

## 第三步：播放端自动配置 + 试运行

播放端安装自动进行（装依赖、内置播放核心，无需手动填写任何东西——口令自动取自服务器，保证一致）。

最后进入**虚拟模式试运行**（不出声，只是验证能连上服务器）：屏幕显示「已连接」即一切正常，按 **Ctrl+C** 退出，脚本会继续注册开机自启。

## 第四步：连蓝牙音箱（一次性）

1. Windows 设置 → 蓝牙 → 配对你办公室的蓝牙音箱
2. 右下角**音量图标 → 输出设备选成那个蓝牙音箱**

（声音是从**电脑**出到蓝牙音箱，跟网页没关系，所以这步在 Windows 里做。）

## 第五步：登录网易云（一次性，必须 VIP 账号）

1. 浏览器打开 http://localhost:3000
2. 顶栏点「🎵 网易云」→ 弹出二维码 → 用**网易云 App** 扫码
3. **必须是 VIP 会员账号**，否则会员歌会自动跳过

## 第六步：同事怎么点歌

1. 同事手机连**办公室同一个 Wi-Fi**
2. 扫屏幕「扫码点歌」的二维码（微信/相机都行，扫出来点一下地址打开）
3. 输歌名 → 点「点歌」，歌就从蓝牙音箱播出来了

---

## 日常使用

- **什么都不用做**：服务器和播放端都注册了开机自启，电脑一开，点歌台自动运行
- 想手动启动：双击 `server\server_start.bat` 和 `player\player_run.bat`（两个黑窗口，别关）
- 想手动停止：把黑窗口关掉即可
- 想取消开机自启：开始菜单 → 启动文件夹 → 删掉「点歌台服务器」「点歌台播放端」两个快捷方式

## 常见问题

| 现象 | 原因 / 解决 |
|---|---|
| 手机打不开页面 | 手机和电脑不在同一个 Wi-Fi；或防火墙放行失败（第一次启动时点「允许访问」） |
| 页面能开但没声音 | Windows 右下角音量图标 → 输出设备没选蓝牙音箱 |
| 歌显示「加载失败，已自动跳过」 | 网易云未登录或登录的不是 VIP 账号 |
| 电脑重启后点歌台没起来 | 双击 `setup_lan_win.bat` 重新注册自启，或直接手动双击两个启动文件 |

---

有问题请联系：追寻AI作品 vx:BMWx5MMMM
